class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hasharr={}
        for i,v in enumerate(nums):
            dif=target-v
            if dif in hasharr:
                return [hasharr[dif],i]
            hasharr[v]=i
        
